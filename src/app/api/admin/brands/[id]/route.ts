import { getAdminUser } from "@/lib/supabase/auth";
import { createClient } from "@/lib/supabase/server";
import { brandSchema } from "@/lib/validations/admin";

export async function PATCH(request: Request, { params }: { params: Promise<{ id: string }> }) { if (!await getAdminUser()) return Response.json({ message: "No autorizado." }, { status: 403 }); try { const { id } = await params; const input = brandSchema.parse(await request.json()); const supabase = await createClient(); const { data, error } = await supabase.from("brands").update(input).eq("id", id).select().single(); if (error) return Response.json({ message: error.code === "23505" ? "Ya existe una marca con ese slug." : "No pudimos guardar la marca." }, { status: 400 }); return Response.json(data); } catch { return Response.json({ message: "Revisa los datos de la marca." }, { status: 400 }); } }
export async function DELETE(_request: Request, { params }: { params: Promise<{ id: string }> }) { if (!await getAdminUser()) return Response.json({ message: "No autorizado." }, { status: 403 }); const { id } = await params; const supabase = await createClient(); const { count } = await supabase.from("products").select("id", { count: "exact", head: true }).eq("brand_id", id); if ((count ?? 0) > 0) { const { error } = await supabase.from("brands").update({ active: false }).eq("id", id); return error ? Response.json({ message: "No pudimos desactivar la marca." }, { status: 400 }) : Response.json({ deactivated: true }); } const { error } = await supabase.from("brands").delete().eq("id", id); return error ? Response.json({ message: "No pudimos eliminar la marca." }, { status: 400 }) : Response.json({ deleted: true }); }
